sx128_t
    state
        rx (enum)
            off, listening, receiving, received, read
        opmode (uint8_t)
        event (uint8_t): rx_done, tx_done, ...

    settings
        channel (uint32_t)
        modem (uint8_t) => packet type enum
        regulator_mode (enum)
        power (int 8)
        ramp_time (enum)
        RX mode (period, single, continuous)
        lora
            preamble length (uint 16)
            spreading factor (uint 8)
            bandwidth (uint 8)
            coderate (iunt 8)
            payload length (uint 8)
            flags (uint 8)
                IQ inverted
                enable fixed header length
                enable crc

    params
        spi device (spi device)
        reset pin (gpio hal pin)
        busy pin (gpio hal pin)
        dio1 pin (gpio hal pin)
        dio2 pin (gpio hal pin)
        dio3 pin (gpio hal pin)
        paselect (int 8) power amplifier mode (RFO or PABOOST)
    _internal
        rx rssi (int 16)
        rx snr (uint 16)
        rx length (uint16)
        rx timestamp (rtimer)
        receive timestamp (rtimer)
        packet[256] (uint8)
    flags (uint 16)
        ? interrupts 


// things to add?
settings: 
    packet type (enum)
    regulator mode (enum)

// use event types instead of variables